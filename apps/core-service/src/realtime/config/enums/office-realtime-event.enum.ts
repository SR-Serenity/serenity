export enum OfficeRealtimeEvent {
  ROOM_CREATED = 'office.room.created',
  ROOM_UPDATED = 'office.room.updated',
  ROOM_DELETED = 'office.room.deleted',
  PARTICIPANT_JOINED = 'office.participant.joined',
  PARTICIPANT_LEFT = 'office.participant.left',
  NOTE_UPDATED = 'office.note.updated',
}
